stages:
  - clean_up
  - build
  
build_and_deploy_prod:
  stage: build
  script:
    - npm install
    - git remote add dokku dokku@www.mehla.in:www
    - git push dokku master
  only:
    - master

clean_up_job:
  stage: clean_up
  script:
    - rm -rf dist/